/* questo metodo scandisce gli elementi di un array cercando un elemento dato .
 * Se l'elemento cercato risulta appartenere all'array il metodo restituisce l'indice della posizione in cui si trova l'elemento chiava
 * nell'array. Se il vettore è nullo , o l'elemento chiave non è contenuto il metodo restituisce -1 */
 
public class RicercaLineareiterativa {

	public static void main(String[] args) {
		int[] A = {1,3,8,6,4,7};
		int chiave = 8;
		Ricerca(A,chiave);
		System.out.println(Ricerca(A,chiave));

	}
public static int[] (int[]A , int chiave){
	for(int i=0;i<A.length;i++)
		if(A==null)
			return -1;
	else{
		if(A[i]==chiave)
			return i;
		else
			return -1;
		
	}
	
}
}
